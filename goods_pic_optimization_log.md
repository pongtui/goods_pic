# 商品取图规则匹配 - 开发优化记录

> **文档用途**：记录本 Skill 开发过程中的所有优化决策、遇到的坑及解决方案，作为后续体系化构建的重要依据。

---

## 版本信息

| 版本 | 日期 | 作者 | 说明 |
|------|------|------|------|
| v1.0 | 2026-05-30 | - | 初始版本 |
| v1.1 | 2026-05-30 | - | 创建 SKILL.md，确立前后端分离架构 |
| v1.2 | 2026-05-30 | - | 参数结构调整：去掉搭配型号、子项改为数组格式 |
| v1.3 | 2026-05-30 | - | 修复型号正则：支持 LS387S10-D 等多段字母数字型号 |
| v1.4 | 2026-05-30 | - | 修复正则误匹配：i标志导致 x4-S 等被错误识别为型号 |
| v1.5 | 2026-05-30 | - | 修复正则：连字符前支持字母结尾（RQ3R-A 中的 R） |

---

## 项目架构

```
goods_pic/
├── .trae/skills/goods-pic-matcher/
│   └── SKILL.md                 ← Agent Skill 定义（规则引擎，后续可部署为 API）
├── goods_pic_matcher.html       ← 可视化验证工具（前端页面，用于快速验证规则）
├── goods_pic_optimization_log.md ← 本文件（开发优化记录）
└── goods_pic_error_log.md       ← 取图错误场景记录
```

**架构说明**：
- `SKILL.md` 是核心规则定义文件，包含完整的匹配算法、输入输出规范、边界情况处理。后续可据此实现服务端 API
- `goods_pic_matcher.html` 是配套的可视化验证页面，在浏览器中打开即可快速测试规则，所见即所得
- 两套文件（优化记录 + 错误记录）分别记录开发过程中的经验教训和实际错误案例

---

## 优化记录

### 优化 #1：型号正则匹配策略

- **日期**：2026-05-30
- **问题描述**：需要从图片名称中自动提取型号（如 `RQ3R-A`、`LS387S10-D`），以判断与入参型号是否完全对应
- **解决方案**：使用正则 `/[A-Z]+\d+[A-Z]*-[A-Z\d]+/g` 提取型号，支持大小写不敏感匹配
- **考虑过的方案**：
  - 方案A：仅用入参型号在图片名中做 `includes` 检查 → 无法检测图片名中"多出"的型号
  - 方案B：维护一个完整的型号字典 → 维护成本高，不够灵活
  - 最终方案：正则提取 + 集合比对（入参型号集合 === 图片名型号集合）
- **注意事项**：
  - 型号可能以 `+` 连接出现在图片名中（如 `实木面+LS387S10-D`）
  - 型号大小写应统一归一化比较（转大写）
  - 正则可能漏掉特殊格式的型号，后续需根据实际数据调整

---

### 优化 #2：多型号输入分隔符处理

- **日期**：2026-05-30
- **问题描述**：入参的型号字段可能包含多个型号，需要合理分割
- **解决方案**：支持逗号（`,`）、中文逗号（`，`）、顿号（`、`）、空格（` `）等多种分隔符
- **注意事项**：
  - 型号本身可能不含这些分隔符，使用 split 不会误伤
  - 搭配型号（`搭配型号`）和主型号（`型号`）统一合并为一个型号集合进行比较

---

### 优化 #3：多次匹配得分相同时的处理

- **日期**：2026-05-30
- **问题描述**：多张图片得分相同时，应该选哪张？
- **解决方案**：V1 版本取第一条最高分图片（即遍历中首次遇到的最高分）
- **待优化**：后续可考虑增加二级排序规则（如匹配顺序优先级、图片名称字典序等）

---

### 优化 #4：关键词匹配的空值处理

- **日期**：2026-05-30
- **问题描述**：颜色/功能/规格字段为空时，匹配逻辑如何处理
- **解决方案**：
  - 若三个字段均为空且有候选图片：返回最后一张候选图片
  - 若仅剩 1 张候选：直接返回，无需打分
  - 若无候选图片：返回"无"
- **注意事项**：空值场景下不要报错，应优雅降级

---

### 优化 #5：图片类型（sku小图）的处理

- **日期**：2026-05-30
- **问题描述**：规则要求图片类型 = "sku小图"，但图片类型信息通常不在图片名称中体现
- **解决方案**：V1 版本中，图片类型仅作记录展示，不直接用于图片名称过滤。实际过滤逻辑应在上游系统（如 CMS 图片库筛选）中完成
- **待优化**：如果后续图片名称中包含类型标识（如 `-SKU-` 后缀），可加入此过滤逻辑

---

### 优化 #6：型号正则的边界情况

- **日期**：2026-05-30
- **问题描述**：图片名中的某些文本可能被误识别为型号
- **解决方案**：
  - 型号正则要求大写字母开头 + 至少一个数字 + 可选的字母 + 连字符 + 字母数字结尾
  - 这能排除大部分普通中文和英文单词
- **已知风险**：
  - 如果图片名中出现类似型号格式但不是型号的文本（如尺寸 `1.3m`），可能不会被误识别
  - 如果型号格式不包含连字符（如 `ABC123`），当前正则会漏掉，需后续补充正则

---

### 优化 #7：创建 Agent Skill 定义文件（SKILL.md）

- **日期**：2026-05-30
- **问题描述**：仅有前端验证页面，缺乏可被 Agent 读取和调用的 Skill 定义文件，后续无法部署为服务端 API
- **解决方案**：
  - 在 `.trae/skills/goods-pic-matcher/SKILL.md` 中创建完整的 Skill 定义
  - 将匹配算法、正则表达式、输入输出格式、边界情况等完整记录在 SKILL.md 中
  - 前端页面 `goods_pic_matcher.html` 作为可视化验证工具，与 Skill 定义互补
- **架构关系**：
  - `SKILL.md` = 规则引擎定义（权威来源），可被 Agent 直接读取并执行
  - `goods_pic_matcher.html` = 可视化验证工具，用于人工快速测试规则正确性
  - 后续部署为 API 时，以 SKILL.md 中的规则为准实现服务端逻辑
- **注意事项**：
  - SKILL.md 和前端 JS 中的匹配逻辑必须保持同步
  - 修改规则时，需同时更新两个文件
  - 前端页面增加了对 SKILL.md 的引用说明

---

### 优化 #8：参数结构调整 — 去掉搭配型号、子项改为数组

- **日期**：2026-05-30
- **问题描述**：
  1. `搭配型号` 是一个冗余字段，和 `型号` 字段在 Step 2 中会合并为一个集合处理，分开管理增加了复杂度
  2. `子项名称`、`子项数量`、`张数` 是三个独立字段，但它们之间存在一一对应的关系（如"餐桌-1-2"、"餐椅-4-3"），独立字段无法表达这种对应关系
- **解决方案**：
  1. **去掉 `搭配型号`**：所有型号统一从 `型号` 字段入参，多个型号用逗号/顿号/空格分隔。Step 2 只从 `params.型号` 读取
  2. **合并为子项数组**：将 `子项名称`、`子项数量`、`张数` 合并为 `子项` 数组，格式为 `[{ "名称": "xx", "数量": "x", "张数": "x" }, ...]`
- **影响范围**：
  - `SKILL.md`：输入参数表、Step 2 算法描述、完整示例
  - `goods_pic_matcher.html`：JSON 输入框 placeholder、提示文字、JS 逻辑、loadExample() 示例
- **注意事项**：
  - 旧版 JSON 仍可解析（`搭配型号` 字段会被忽略，`子项名称`/`子项数量`/`张数` 独立字段不再读取）
  - 子项数组当前版本仅作记录展示，不参与匹配算法

---

### 优化 #9：修复型号正则 — 支持多段字母+数字的型号

- **日期**：2026-05-30
- **问题描述**：旧正则 `/[A-Z]+\d+[A-Z]*-[A-Z\d]+/g` 无法匹配 `LS387S10-D` 这类型号。该型号在连字符前有两段 "字母+数字"（`LS387` + `S10`），旧正则在匹配到 `LS387S` 后期望遇到 `-` 但遇到了 `1`，导致匹配失败
- **错误现象**：所有包含 `LS387S10-D` 型号的图片在 Step 2 中被错误淘汰
- **正则追踪**（旧正则匹配 `LS387S10-D`）：
  - `[A-Z]+` → `LS` ✓
  - `\d+` → `387` ✓
  - `[A-Z]*` → `S` ✓
  - `-` → 期待 `-`，但下一个字符是 `1` → ✗ 失败
- **解决方案**：将正则改为 `/[A-Z]+\d+([A-Z]+\d+)*-[A-Z\d]+/gi`
  - 用 `([A-Z]+\d+)*` 替代 `[A-Z]*`，支持 0 或多段 "字母+数字" 组合
  - 同时添加 `i` 标志，支持小写型号匹配
- **影响范围**：
  - `goods_pic_matcher.html`：MODEL_PATTERN 常量
  - `SKILL.md`：正则说明和示例

---

### 优化 #10：修复 i 标志导致的 false positive — 单字母被误识别为型号

- **日期**：2026-05-30
- **问题描述**：在优化 #9 中为正则添加了 `i` 标志以支持小写型号匹配，但这导致图片名中的 `x4-S`（来自 `餐椅x4-SKU`）被误识别为型号。因为 `i` 标志让 `[A-Z]+` 匹配了 `x`，加上 `\d+` 匹配 `4`、`-` 匹配连字符、`[A-Z\d]+` 匹配 `S`，形成了完整的假阳性匹配
- **错误现象**：所有包含 `x4`、`x2` 等相似模式的图片名在 Step 2 中被错误淘汰（因为 "多了型号"）
- **解决方案**：将 `[A-Z]+` 改为 `[A-Z]{2,}`，要求型号开头至少 2 个字母。`x4-S` 中 `x` 只有 1 个字母 → 不再匹配。同时验证 `RQ3R-A`（`RQ`=2字母）、`LS387S10-D`（`LS`=2字母）不受影响
- **最终正则**：`/[A-Z]{2,}\d+([A-Z]+\d+)*-[A-Z\d]+/gi`
- **影响范围**：
  - `goods_pic_matcher.html`：MODEL_PATTERN
  - `SKILL.md`：正则说明
- **注意事项**：如果存在 `A1-B` 这类单字母前缀的型号，当前正则将无法匹配。目前项目中的型号均为至少 2 字母前缀

---

### 优化 #11：修复正则 — 连字符前允许字母结尾（RQ3R-A 无法匹配）

- **日期**：2026-05-30
- **问题描述**：v1.4 正则 `([A-Z]+\d+)*` 要求连字符前每段可选组必须以数字结尾，导致 `RQ3R-A`（连字符前为 `RQ3R`，尾字符是 `R`）无法匹配
- **正则追踪**（旧正则匹配 `RQ3R-A`）：
  - `[A-Z]{2,}` → `RQ` ✓
  - `\d+` → `3` ✓
  - 位置 3 是 `R`。`([A-Z]+\d+)*` 尝试：`[A-Z]+` 匹配 `R`，`\d+` 找不到数字 → 内层组失败。`*` 回到匹配 0 次。
  - 位置仍为 3，期待 `-` 但字符是 `R` → ✗ 失败
- **错误现象**：所有 `RQ3R-A`、`RQ3R-B` 等连字符前以字母结尾的型号全部无法提取，Step 2 无法匹配任何图片
- **解决方案**：改为 `/[A-Z]{2,}\d+([A-Z]*\d+)*[A-Z]*-[A-Z\d]+/gi`
  - `([A-Z]*\d+)*` 中的 `[A-Z]*` 允许 0 个字母（避免贪婪消耗尾字母）
  - 新增 `[A-Z]*` 匹配连字符前允许的结尾字母段
- **影响范围**：
  - `goods_pic_matcher.html`：MODEL_PATTERN
  - `SKILL.md`：正则说明

---

## 技术决策记录

| 决策编号 | 决策内容 | 原因 | 影响 |
|----------|----------|------|------|
| TD-001 | 使用单文件 HTML 实现前端工具 | 无需构建环境，开箱即用，便于分享 | 后续复杂化时可能需要拆分 |
| TD-002 | 型号匹配使用正则自动提取 | 避免手动维护型号字典 | 可能存在正则覆盖不全的情况 |
| TD-003 | 所有比较统一转为大写（normalize） | 型号大小写不应影响匹配结果 | - |
| TD-004 | SKILL.md + HTML 前后端分离架构 | Skill 定义是权威来源，HTML 是验证工具，职责分离便于后续 API 化 | 修改规则时需同步更新两个文件 |

---

## 待办事项

- [ ] 补充更多型号格式的正则支持（无连字符型号、纯数字型号等）
- [ ] 增加得分相同时的二级排序策略
- [ ] 支持图片类型字段的图片名过滤（如果图片名包含类型标识）
- [ ] 增加匹配结果导出功能
- [ ] 考虑将匹配规则抽象为可配置的规则引擎

---
