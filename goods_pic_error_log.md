# 商品取图规则匹配 - 错误场景记录

> **文档用途**：记录取图规则匹配过程中出现的错误场景，包括输入参数、输出结果、错误原因和修复方案，用于后续规则迭代和回归验证。

---

## 使用说明

每一条错误记录应包含以下信息：
- **错误编号**：唯一标识，格式 `ERR-YYYYMMDD-NNN`
- **发现日期**：发现该错误的日期
- **严重程度**：致命 / 严重 / 一般 / 轻微
- **输入参数**：完整的取图参数 JSON
- **图片名称列表**：所有输入的图片名称
- **期望结果**：预期的匹配结果
- **实际结果**：工具实际返回的结果
- **错误原因**：分析导致错误的原因
- **修复方案**：采取的修复措施
- **修复日期**：修复完成的日期
- **状态**：待修复 / 已修复 / 已验证 / 不予修复

---

## 错误记录

### 模板（复制使用）

```markdown
### ERR-YYYYMMDD-001：错误简述

| 字段 | 内容 |
|------|------|
| **错误编号** | ERR-YYYYMMDD-001 |
| **发现日期** | YYYY-MM-DD |
| **严重程度** | 一般 |
| **状态** | 待修复 |

**输入参数**：
\`\`\`json
{
  "渠道": "",
  "销售类目": "",
  "图片类型": "sku小图",
  "型号": "",
  "颜色": "",
  "功能": "",
  "规格": "",
  "子项": []
}
\`\`\`

**图片名称列表**：
\`\`\`
\`\`\`

**期望结果**：
\`\`\`
\`\`\`

**实际结果**：
\`\`\`
\`\`\`

**错误原因**：


**修复方案**：


**修复日期**：-
```

---

## 记录列表

### ERR-20260530-001：型号正则无法匹配 LS387S10-D 等多段字母数字型号

| 字段 | 内容 |
|------|------|
| **错误编号** | ERR-20260530-001 |
| **发现日期** | 2026-05-30 |
| **严重程度** | 严重 |
| **状态** | 已修复 |

**输入参数**：
```json
{
  "渠道": "天猫",
  "销售类目": "家具",
  "图片类型": "sku小图",
  "型号": "RQ3R-A, LS387S10-D",
  "颜色": "实木面",
  "功能": "伸缩",
  "规格": "1.3m",
  "子项": []
}
```

**图片名称列表**：
```
RQ3R-A组合-主图-1.3m伸缩餐桌-实木面+LS387S10-D餐椅
RQ3R-B组合-主图-1.3m伸缩餐桌-实木面+LS387S10-D餐椅x4-SKU-(1).jpg
RQ3R-A组合-主图-1.3m伸缩餐桌-大理石面+LS387S10-D餐椅x4-SKU-(1).jpg
```

**期望结果**：
图片 1 和图片 3 应通过型号过滤（型号完全对应 RQ3R-A + LS387S10-D）

**实际结果**：
三张图片全部被淘汰，因为正则无法提取 `LS387S10-D`

**错误原因**：
旧正则 `/[A-Z]+\d+[A-Z]*-[A-Z\d]+/g` 要求 "字母+数字+可选字母+连字符" 的结构。`LS387S10-D` 在连字符前有 `S10`（第二段字母+数字），导致匹配失败。

**修复方案**：
将正则改为 `/[A-Z]+\d+([A-Z]+\d+)*-[A-Z\d]+/gi`，用 `([A-Z]+\d+)*` 支持多段字母+数字，同时加 `i` 标志支持小写。

**修复日期**：2026-05-30

---

### ERR-20260530-002：i 标志导致 x4-S 被误识别为型号（false positive）

| 字段 | 内容 |
|------|------|
| **错误编号** | ERR-20260530-002 |
| **发现日期** | 2026-05-30 |
| **严重程度** | 严重 |
| **状态** | 已修复 |

**输入参数**：
```json
{
  "渠道": "天猫",
  "销售类目": "家具",
  "图片类型": "sku小图",
  "型号": "RQ3R-A, LS387S10-D",
  "颜色": "实木面",
  "功能": "伸缩",
  "规格": "1.3m",
  "子项": []
}
```

**图片名称列表**：
```
RQ3R-A组合-主图-1.3m伸缩餐桌-实木面+LS387S10-D餐椅x4-SKU-(1).jpg
```

**期望结果**：
该图片应通过型号过滤（型号集合完全对应 RQ3R-A + LS387S10-D）

**实际结果**：
该图片被淘汰，提示"多了型号：x4-S"

**错误原因**：
在 ERR-20260530-001 的修复中为正则添加了 `i` 标志（大小写不敏感），导致 `餐椅x4-SKU` 中的 `x4-S` 被完整匹配为型号：
- `x` → `[A-Z]+`（i 标志）→ 匹配
- `4` → `\d+` → 匹配
- `-` → 连字符 → 匹配
- `S` → `[A-Z\d]+` → 匹配

**修复方案**：
将 `[A-Z]+` 改为 `[A-Z]{2,}`，要求型号开头至少 2 个字母。`x` 只有 1 个字母 → 不再误匹配。

**修复日期**：2026-05-30

---

### ERR-20260530-003：正则无法匹配 RQ3R-A（连字符前以字母结尾的型号）

| 字段 | 内容 |
|------|------|
| **错误编号** | ERR-20260530-003 |
| **发现日期** | 2026-05-30 |
| **严重程度** | 致命 |
| **状态** | 已修复 |

**输入参数**：
```json
{
  "渠道": "天猫",
  "销售类目": "家具",
  "图片类型": "sku小图",
  "型号": "RQ3R-A, LS387S10-D",
  "颜色": "实木面",
  "功能": "伸缩",
  "规格": "1.3m",
  "子项": []
}
```

**图片名称列表**：
```
RQ3R-A组合-主图-1.3m伸缩餐桌-实木面+LS387S10-D餐椅x4-SKU-(1).jpg
```

**期望结果**：
该图片应提取到型号 `["RQ3R-A", "LS387S10-D"]`，通过 Step 2 型号过滤

**实际结果**：
只提取到 `["LS387S10-D"]`，缺失 `RQ3R-A`，被错误淘汰

**错误原因**：
v1.4 正则 `([A-Z]+\d+)*` 要求每段可选组必须以数字结尾。匹配 `RQ3R-A` 时：`RQ`→`3` 之后遇到 `R`，`([A-Z]+\d+)*` 中的 `[A-Z]+` 消耗 `R` 后 `\d+` 找不到数字 → 内层组失败，`*` 回退到 0 次匹配。此时位置仍在 `R`，期待 `-` 但拿到 `R` → 整体失败。

**修复方案**：
改为 `/[A-Z]{2,}\d+([A-Z]*\d+)*[A-Z]*-[A-Z\d]+/gi`：
- `([A-Z]*\d+)*` 允许 0 字母开头（避免贪婪消耗尾字母）
- 新增 `[A-Z]*` 匹配连字符前的结尾字母段

**修复日期**：2026-05-30

---

（暂无其他记录）

---
